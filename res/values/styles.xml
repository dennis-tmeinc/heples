<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.DeviceDefault.Light">

        <item name="android:listPopupWindowStyle">@style/MyListPopupWindow</item>
    </style>

    <style name="MyListPopupWindow" parent="android:Widget.Holo.Light.ListPopupWindow">
        <item name="android:divider">@android:color/holo_blue_dark</item>
    </style>

</resources>
